# Generated by confd 

ProxyStatus Full
ExtendedStatus On

# default way:
#ProxyPass /examples balancer://examples stickysession=JSESSIONID

# test to try that the balancer works:
ProxyPass /examples balancer://examples

<Proxy balancer://examples>
{{range gets "/docker-services/docker_tomcatservice*-8080/*"}}
    {{$tomHost := index (split (base .Key) "_") 1}} 
	BalancerMember ajp://{{$tomHost}}:8009/examples route={{$tomHost}}
{{end}}
</Proxy>
